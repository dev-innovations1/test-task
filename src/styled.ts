import { styled } from "@mui/material";

export const MainLayout = styled("div")(() => ({
  background: "white",
  display: "flex",
  flexDirection: "column",
  height: "100vh",
  overflow: "hidden",
  padding: "32px",
}));
